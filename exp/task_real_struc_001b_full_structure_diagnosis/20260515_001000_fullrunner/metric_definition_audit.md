# metric_definition_audit

Overall NMSE uses full normalized 24^3 volumes. Hard-region NMSE uses the same denominator convention restricted to deterministic support masks. Support diagnostics use GT-derived support only for evaluation, not training.
