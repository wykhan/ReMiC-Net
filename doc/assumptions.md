# assumptions

The following high-level facts are treated as frozen for the bootstrap stage:

- The project is grounded in true cylindrical physical simulation
- The current learning target is amplitude reconstruction
- The default mainline is `RED_ref3 -> 3D U-Net -> GT amplitude`
- Point targets are used for prerequisite validation
- Extended targets are the main scientific battlefield
- `CONTEXT/simulation_protocol.md` and `CONTEXT/reference_surface_strategy.md` are the active frozen protocols
